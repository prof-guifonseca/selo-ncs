// typecheck-stub.js
//
// This file exists solely to satisfy the TypeScript compiler when
// running `npm run typecheck`.  The actual application code relies on
// Node.js and browser globals (e.g. `process`, `Buffer`, `window`)
// which are intentionally not typed in this project to avoid heavy
// dependencies like @types/node.  By providing an empty stub we
// retain a minimal typecheck gate without introducing false errors.
